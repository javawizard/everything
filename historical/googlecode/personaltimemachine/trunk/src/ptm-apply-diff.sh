#!/bin/bash

# Takes three arguments, basedir, revision, and file. applies the diff at 
# basedir/diffs/revision, searching for the proper line file, to the file in question,
# in the current directory.

