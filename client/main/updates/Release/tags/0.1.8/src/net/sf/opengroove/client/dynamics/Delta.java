package net.sf.opengroove.client.dynamics;

/**
 * A delta is a set of commands, a timestamp that represents when they were
 * created, and an identifier (typically an OpenGroove userid when used within
 * OpenGroove) that indicates the user that created this delta.
 * 
 * @author Alexander Boyd
 * 
 */
public class Delta
{
    
}
